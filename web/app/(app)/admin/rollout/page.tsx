import { StateRolloutAdmin } from "./state-rollout-admin";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "State Rollout | Admin",
};

export default function AdminStateRolloutPage() {
  return <StateRolloutAdmin />;
}
